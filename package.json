{
  "name": "emailfooter",
  "version": "1.0.1",
  "description": "email footer",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "scripts": {
  	"pack": "npm --no-git-tag-version version patch && node zip.pack.js"
  },
  "devDependencies": {
    "bestzip": "^2.1.2"
  }
}
